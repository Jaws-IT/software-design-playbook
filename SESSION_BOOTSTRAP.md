# Session Bootstrap (Copy/Paste)

Use this at the start of any new AI coding session:

```text
Load and apply /Users/jespersorensen/IdeaProjects/jaws-it/software-design-playbook/OPERATING_PROMPT.md as the governing workflow for this session.
Use the default loop: implement -> detect violations -> repair -> detect -> repeat until clean (or explicitly blocked).
Use the playbook agents and required detection prompts referenced in that file.
If this task is a prototype/spike, ask me to confirm an exception scope/timebox before relaxing enforcement.
```

## MCP Version

If exposed as an MCP resource, use equivalent wording with the MCP URI/resource name in place of the local path.
